import { ICOTracker } from '@/components/web3/ico-tracker';
import { AppLayout } from '@/layouts/app-layout';

export default function ICOTrackerPage() {
  return (
    <AppLayout>
      <ICOTracker />
    </AppLayout>
  );
}