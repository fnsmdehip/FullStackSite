import { GasTracker } from '@/components/web3/gas-tracker';
import { AppLayout } from '@/layouts/app-layout';

export default function GasTrackerPage() {
  return (
    <AppLayout>
      <GasTracker />
    </AppLayout>
  );
}